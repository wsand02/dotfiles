Future William here, install ohmyzsh and p10k first.

These have been replaced with forked versions:
```sh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/wsand02/ohmyzsh/master/tools/install.sh)"
```

```sh
git clone --depth=1 https://github.com/wsand02/powerlevel10k.git "${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k"
```
